- Use pnpm.
- The frontend's visual design system (colour tokens, typography, the status-icon vocabulary,
  layout and stylesheet conventions) lives in [`DESIGN.md`](./DESIGN.md). It's the source of
  truth for how the dashboard should look — consult it before styling anything new, and keep
  it updated when the design changes.

